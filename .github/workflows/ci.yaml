name: CI

on:
  pull_request:
  push:
    branches: ['main']

jobs:
 status-check:
    name: Github Status Check
    runs-on: ubuntu-latest
    if: always() && !cancelled()
    steps:
      - name: Always succeed
        run: echo "Status check passed"
