v31_draft_with_main_server:
  era_contracts: kl/l2V31upgrade
  zksync_os_server: main
  tests:
    - l1_settling_test
    - gateway_settling_test
    - interop_test
    - add_remove_validator_test

migrate_live_chain_gateway:
  era_contracts: kl/l2V31upgrade
  zksync_os_server: gateway-live-migration-fixes
  tests:
    - migrate_live_chain_from_gateway_test
    - migrate_live_chain_to_gateway_test

v30_to_v31_upgrade:
  era_contracts: kl/l2V31upgrade
  # TODO: revert to `main` once the factory_deps + preimage changes on this
  # branch have landed and a main image with them has been published. Using
  # the local checkout here so server-side edits (debug logging,
  # upgrade_tx_watcher tweaks) are exercised by the upgrade test.
  zksync_os_server: gateway-live-migration-fixes
  tests:
    - upgrade_v30_to_v31
